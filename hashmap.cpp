#include <bits/stdc++.h>
using namespace std;

/**
 * HashMap
 * - implementing a hashmap for mapping tick symbols to uint32_t's 
 * - collisions are supposed to be dealt with using linear probing
 * - supporting deletions require tombstones
 * - need special values that make a slot as empty (or taken, or tombstone)
 */

template<typename K, typename V>
class HashMap {
public:
    HashMap() {

    };
private:

};

int main() {

}